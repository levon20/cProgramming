#include "matrixCalc"

double* MatrixCalc(double* p0, double* p1, int size, char operation)
{
	double* reasult = malloc();
	
	for(int i = 0; i < size; i++)
	{
		for(int j = 0; j < size; j++)
		{
			if(operation == '+')
							
			else if(operation == '-')
	
			else
		}
	}
	
}